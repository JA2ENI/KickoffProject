<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.teamcommit.kickoff.Service.reply.ReplyMapper">

	<!-- Reply Insert -->
    <insert id="insertReply" parameterType="com.teamcommit.kickoff.Do.ReplyDO">
        <![CDATA[
        INSERT INTO Reply
        (
            REPLY_NO,
            REPLY_ID,
            BOARD_SEQNO,
            REPLY_REG_DATE,
            REPLY_CONTENT,
            REPLY_ID_EMP
        )
        VALUES
            (
                REPLY_NO.NEXTVAL,
                #{replyId},
                #{boardSeqno},
                #{replyRegDate},
                #{replyContent},
                #{writeContent},
                #{replyIdEmp}
            )
        ]]>
    </insert>
    
    <!-- Reply Read -->
    <select id="readReply" parameterType="com.teamcommit.kickoff.Do.ReplyDO" resultType="com.teamcommit.kickoff.Do.ReplyDO">
        <![CDATA[
        SELECT
            *
        FROM
            REPLY
       	WHERE
       		REPLY_NO = #{replyNo}
        ]]>
    </select>
    
    <!-- Reply Delete -->
    <delete id="deleteReply" parameterType="com.teamcommit.kickoff.Do.ReplyDO" >
    <![CDATA[
        DELETE
        FROM
            REPLY
        WHERE
            REPLY_NO = #{replyNo}
        ]]>
	</delete>
	
	<!-- Reply Update -->
    <update id="updateBoard" parameterType="com.teamcommit.kickoff.Do.ReplyDO">
        <![CDATA[
        UPDATE
            REPLY
        SET
            REPLY_CONTENT = #{replyContent},
            REPLY_EDIT_DATE = #{replyEditDate}
        WHERE
            REPLY_NO = #{replyNo}
        ]]>
    </update>
    
    <!-- Reply List -->
    <select id="getList" parameterType="com.teamcommit.kickoff.Do.ReplyDO" resultType="com.teamcommit.kickoff.Do.ReplyDO">
        <![CDATA[
        SELECT
            *
        FROM
            REPLY
       	WHERE
       		BOARD_SEQNO = #{boardSeqno}
       	ORDERR BY REPLY_NO ASC;
        ]]>
    </select>

</mapper>