package com.teamcommit.kickoff.Controller;

import javax.annotation.Resource;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.context.annotation.Conditional;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;

import com.teamcommit.kickoff.Service.login.LoginService;

@Controller
public class SignupController {

	/*
	 * @Autowired
	 * 
	 * @Qualifier("LoginService") private LoginService loginService;
	 * 
	 * @RequestMapping("/Signup") public String Signup() { String view =
	 * "/login/Signup";
	 * 
	 * return view; }
	 * 
	 * @RequestMapping("/SignupEmp") public String SignupEmp() { String view =
	 * "/login/SignupEmp";
	 * 
	 * return view; }
	 */
}
